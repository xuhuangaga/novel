/* 设置元素display属性为flex */
.flex {
  display: flex;
}

/* 元素水平方向居中显示 */
.j-center {
  justify-content: center;
}

/* 元素两侧显示 */
.j-between {
  justify-content: space-between;
}

/* 元素内容平均显示 */
.j-around {
  justify-content: space-around;
}

/* 垂直方向居中显示 */
.a-center {
  align-items: center;
}

/* 垂直方向底部齐平显示 */
.a-i-end {
  align-items: flex-end;
}



/* 
  设置元素display属性为flex
  元素水平方向居中显示
   */
.flex-j-center {
  display: flex;
  justify-content: center;
}

/* 
  设置元素display属性为flex
  元素水平方向居右显示
   */
.flex-j-end {
  display: flex;
  justify-content: flex-end;
}

/* 
  设置元素display属性为flex
  元素垂直方向居中显示
   */
.flex-a-center {
  display: flex;
  align-items: center;
}

/* 元素内容支持换行 */
.f-wrap {
  flex-wrap: wrap;
}
/* 文本内容支持换行 */
.w-wrap {
  word-wrap:break-word;
}
/* 
  设置元素display属性为flex
  元素两侧方向显示
   */
.flex-j-between {
  display: flex;
  justify-content: space-between;
}

/* 
  设置元素display属性为flex
  元素平均显示
   */
.flex-j-around {
  display: flex;
  justify-content: space-around;
}

/* 字体大小为12px */
.f-size12 {
  font-size: 12px;
}

/* 字体大小为14px */
.f-size14 {
  font-size: 14px;
}

/* 字体大小为16px */
.f-size16 {
  font-size: 16px;
}

/* 字体大小为20px */
.f-size20 {
  font-size: 20px;
}

/* 字体颜色为白色 */
.f-c-w {
  color: #fff;
}

/* 字体颜色为浅灰色 */
.f-c-c {
  color: #ccc;
}

/* 字体加粗 */
.f-w-bold {
  font-weight: bold;
}

/* 设置padding为10px */
.p-10 {
  padding: 20rpx;
}


/* 设置padding为20px */
.p-20 {
  padding: 40rpx;
}

/* 设置padding为5px */
.p-5 {
  padding: 10rpx;
}

/* 设置padding左右为5px */
.p-l-r5 {
  padding: 0rpx 10rpx;
}

/* 设置padding左边为10px */
.p-l10 {
  padding-left: 20rpx;
}

/* 设置padding左边为15px */
.p-l15 {
  padding-left: 30rpx;
}

/* 设置padding右边为10px */
.p-r10 {
  padding-right: 20rpx;
}

/* 设置padding右边为0px */
.p-r0 {
  padding-right: 0rpx;
}

/* 设置padding左右为10px */
.p-l-r10 {
  padding: 0rpx 20rpx;
}

/* 设置padding左右为20px */
.p-l-r20 {
  padding: 0rpx 40rpx;
}

/* 设置padding上面为10px */
.p-t10 {
  padding-top: 20rpx;
}

/* 设置padding上下为10px */
.p-tb10 {
  padding: 20rpx 0rpx;
}

/* 设置padding上下为20px */
.p-tb20 {
  padding: 40rpx 0rpx;
}

/* 设置padding上为20px */
.p-t20 {
  padding-top: 40rpx;
}

/* padding-bottom为20px */
.p-b20 {
  padding-bottom: 40rpx;
}

/* padding-bottom为10px */
.p-b10 {
  padding-bottom: 20rpx;
}

/* 设置margin为10px */
.m-10 {
  margin: 20rpx;
}


/* 设置margin-top为10px */
.m-t10 {
  margin-top: 20rpx;
}
/* 设置margin-top为5px */
.m-t5 {
  margin-top: 10rpx;
}
/* 设置margin-bottom为10px */
.m-b10 {
  margin-bottom: 20rpx;
}

/* 设置margin-right为20px */
.m-r20 {
  margin-right: 40rpx;
}

/* 设置margin-right为5px */
.m-r-5 {
  margin-right: 10rpx;
}
/* 设置margin-right为5px */
.m-r5 {
  margin-right: 10rpx;
}

/* 设置margin-left为10px */
.m-l10 {
  margin-left: 20rpx;
}

/* 设置margin左边和右边为10px */
.m-lr10 {
  margin: 0rpx 20rpx;
}

/* 设置margin左边和右边为5px */
.m-lr5 {
  margin: 0rpx 10rpx;
}

/* 设置margin-top为5px */
.m-t5 {
  margin-top: 10rpx;
}

/* 设置margin上下为10px */
.m-t-b10 {
  margin: 20rpx 0rpx;
}

/* 设置margin上下为5px */
.m-t-b5 {
  margin: 10rpx 0rpx;
}

/* 设置margin上下左右为15px */
.m-15 {
  margin: 30rpx;
}

/* 设置margin上下左右为20px */
.m-20 {
  margin: 40rpx;
}

/* 设置margin上为20px */
.m-t20 {
  margin-top: 40rpx;
}

/* 设置margin下为10px */
.m-b10 {
  margin-bottom: 20rpx;
}

/* 设置margin下为20px */
.m-b20 {
  margin-bottom: 40rpx;
}

/* 设置margin上下为20px */
.m-t-b20 {
  margin: 40rpx 0rpx;
}

/* 设置margin-right为10px */
.m-r10 {
  margin-right: 20rpx;
}

/* 设置margin-right为0px */
.m-r0 {
  margin-right: 0rpx;
}

/* 圆点 */
.yd {
  border-radius: 100%;
}

/* 文本内容水平居中 */
.t-a-center {
  text-align: center;
}

/* 文本内容居右显示 */
.t-a-right {
  text-align: right;
}

/* 相对定位 */
.p-relative {
  position: relative;
}
/* 背景颜色为白色 */
.bc-w {
  background-color: #fff;
}

/* 居中 */
.m-auto {
  margin: 0 auto;
}


/* 文本内容超过两行省略号显示 */
.sl-two {
  text-overflow: -o-ellipsis-lastline;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}
/* 文本内容超过三行省略号显示 */
.sl-three {
  text-overflow: -o-ellipsis-lastline;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
}

/* 文本内容超过宽度省略号显示 */
.sl-one {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* 行间距20px */
.lh20 {
  line-height: 40rpx;
}

/* 行间距30px */
.lh30 {
  line-height: 60rpx;
}

/* 设置左上和右上的border-radius为5px */
.b-t-lr-r-5 {
  border-top-left-radius: 10rpx;
  border-top-right-radius: 10rpx;
}

/* 设置没有下划线 */
.t-d-none {
  text-decoration: none;
}

/* div文本内容超出自动换行 */
.d-wrap {
  word-wrap: break-word;
  word-break: break-all;
  overflow: hidden;
}

/* 宽度百分百 */
.wbfb {
  width: 100%;
}

/* 高度百分百 */
.hbfb {
  height: 100%;
}

/* 超出部分隐藏 */
.o_hidden {
  overflow: hidden;
}

/* 设置固定定位 */
.p_fixed {
  position: fixed;
}

.border_b {
  border-bottom: 1px solid #ccc;
}

.t_c {
  color: #7f7f7f;
}
.tc_6 {
  color: #666;
}
.tc_9 {
  color: #999;
}
.tc_theme {
  color: #cb1c36;
}
.clear_img {
  width: 40rpx;
  height: 40rpx;
}
.tc_orange{
  color:#ff6347
}
.book_img {
  flex: 1;
  width: 240rpx;
}
.book_img image {
  width: 240rpx;
}
.content_dv {
  flex: 2;
}
.search_item {
  height: 320rpx;
}
.tab_dv {
  flex: 1;
}
.active_item {
  color: #ec304c;
  border-bottom: 4rpx solid #ec304c;
}
.tab {
  width: fit-content;
}
.w-m-content {
  width: max-content;
}
.m-t-tabs {
  padding-top: 100rpx;
}
.bookshelf {
  height: 320rpx !important;
  width: 220rpx !important;
}